import 'package:flutter/material.dart';

class MoBanWaiKe extends StatelessWidget {
  const MoBanWaiKe({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('处方模板：后续接入'));
}
